[pytest]
testpaths = tests
# Les modules core sont dans jarvis_core/ : on l'ajoute (avec la racine pour
# jarvis_actions/jarvis_skills) au sys.path des tests, qui les importent a plat.
pythonpath = . jarvis_core
addopts = -q
asyncio_default_fixture_loop_scope = function
